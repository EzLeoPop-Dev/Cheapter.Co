import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const count = await prisma.book.count(); const activeCount = await prisma.book.count({ where: { NOT: { OR: [ { sampleData: { path: ['adminStatus'], equals: 'draft' } }, { sampleData: { path: ['adminStatus'], equals: 'discontinued' } }, { AND: [ { NOT: { sampleData: { path: ['adminStatus'], equals: 'active' } } }, { stock: { lte: 0 } } ] } ] } } }); console.log('Total:', count, 'Active:', activeCount); } main().finally(() => prisma.$disconnect());
